# AI-система автоматизации обработки тикетов поддержки

Решение задания 2 (System Design): проектирование AI/ML-системы обработки тикетов поддержки крупного онлайн-сервиса (~200k тикетов/день) + минимальный PoC.

## Что делает решение

Входящий тикет проходит быстрый синхронный путь (PII-маскирование → классификация темы и риска → маршрутизация, ≤ 500 мс) и, если он типовой и безопасный, — асинхронный путь (retrieval по базе знаний → черновик ответа → gate). Рискованные и low-confidence тикеты **всегда эскалируются оператору**, все решения пишутся в audit log.

## Зачем это бизнесу

Около 40% из 200k ежедневных тикетов — типовые, и операторы тратят на них по 150 ₽ и 8 минут каждый. Система снимает рутину с операторов: черновики и маршрутизация ускоряют обработку сразу, а автозакрытие безопасных категорий на поздних этапах раскатки убирает оператора из цикла полностью. При автоматизации 20% потока чистый эффект — порядка 1 млрд ₽/год (расчёт в [docs/product.md](docs/product.md)). При этом безопасность не приносится в жертву: рискованные категории всегда проверяет человек, а guardrail-метрики (reopen, CSAT) останавливают раскатку при деградации.

## Как запустить PoC

Требуется только Python 3.10+ (stdlib, без зависимостей):

```bash
python3 poc/demo.py              # happy path + risky path на 7 mock-тикетах
python3 poc/demo.py --llm-down   # fallback: LLM API недоступен
python3 poc/demo_loadguard.py    # пик нагрузки: дедупликация инцидента + бюджет LLM
python3 -m unittest discover poc -v   # smoke-тесты (19 шт.)
```

Результаты: `poc/out/audit_log.jsonl` (лог решений), `poc/out/operator_inbox.jsonl` (очередь оператора).

## Какой сценарий демонстрируется

1. **Happy path**: типовой тикет про доставку → PII-маскирование → классификация (`delivery`, high confidence) → retrieval релевантной статьи KB → черновик проходит выходной safety/groundedness-gate → черновик ответа в очередь оператора (suggest-режим) → запись в audit log.
2. **Risky path**: тикет про двойное списание с номером карты → PII замаскирован → категория `payment` из risk-списка → эскалация оператору **без** генерации автоответа.
3. **Fallback**: при `--llm-down` генерация деградирует до retrieval-only ответа (ссылки на KB) или эскалации — приём и классификация не страдают.
4. **Пик нагрузки** (`demo_loadguard.py`): всплеск из 17 тикетов об одном сбое доставки схлопывается в кластер — LLM вызывается один раз для лидера, 14 последователей переиспользуют его ответ без генерации; при исчерпании бюджета LLM генерация деградирует до retrieval-only.

## Что реализовано, а что — дизайн

| Реализовано в PoC | Архитектурный дизайн (docs/) |
|---|---|
| PII-маскирование (regex) | + NER-модель |
| Классификатор правил + confidence | TF-IDF+логрег → лёгкий transformer |
| TF-IDF retrieval по мини-KB | embeddings + pgvector/Qdrant |
| Mock-LLM (шаблоны) + промпт-изоляция + выходной gate (PII/groundedness) | LLM API за circuit breaker; adversarial-тесты, LLM-judge groundedness |
| Последовательные вызовы | очереди Kafka/RabbitMQ, sync/async разделение |
| Дедуп (Jaccard) + бюджет LLM (счётчик) | embeddings+LSH кластеризация, распределённый бюджет в Redis |
| JSONL audit log и operator inbox | append-only хранилище, интеграция с helpdesk |

## Допущения и ограничения

- Категории и risk-список заданы вручную по ключевым словам — в реальности обучаются на исторических тикетах (см. [docs/ml.md](docs/ml.md)).
- KB — 5 статей для демонстрации grounding-контракта.
- Confidence PoC-классификатора — эвристика; в целевой системе — калиброванная вероятность модели.
- Автоотправка ответа пользователю в PoC не включена вовсе: всё идёт через оператора, как на suggest-этапе раскатки.

## Документация

- [docs/architecture.md](docs/architecture.md) — целевая архитектура, диаграммы, fallback-пути
- [docs/ml.md](docs/ml.md) — ML/LLM-дизайн, baselines, валидация, разметка
- [docs/monitoring.md](docs/monitoring.md) — метрики, алерты, деградация vs дрейф, стоимость LLM
- [docs/risks-and-ops.md](docs/risks-and-ops.md) — highload, privacy, safety
- [docs/product.md](docs/product.md) — гипотезы, метрики, расчёт эффекта, план раскатки
- [AI_USAGE.md](AI_USAGE.md) · [WORKLOG.md](WORKLOG.md) · [SELF_REVIEW.md](SELF_REVIEW.md)
