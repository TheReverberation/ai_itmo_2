# Использование AI в работе над заданием

Основной инструмент — **Claude Code** (агентный режим). AI использовался на всех этапах, роль на каждом — ниже. Использование AI разрешено условиями задания.

## Где и как использовался AI

- **Понимание задачи и декомпозиция:** AI разобрал PDF задания, выделил обязательные артефакты для программы «ИИ» и предложил тайм-бокс по шагам с приоритетом «документация решений > объём кода». План зафиксирован до начала работы и виден в истории коммитов.
- **Проектирование архитектуры:** AI предложил базовую схему (разделение горячего/холодного пути, очередь, HITL, circuit breaker) — я её принял, поскольку она напрямую следует из ограничений кейса (500 мс, деградация при недоступности LLM, аудит).
- **Выбор ML/LLM-подходов:** каскад «правила → классическая ML → эмбеддинги → LLM» и принцип «LLM не на hot path» сформулированы совместно; решение о целевом recall ≥ 0.95 для risk-классификации как главной ML-метрике — осознанный выбор из-за асимметрии цены ошибки.
- **Разработка PoC:** код пайплайна, demo и тестов написан AI по согласованной структуре; я задал ограничение «только stdlib, без pip install» — чтобы проверяющий гарантированно запустил PoC одной командой.
- **Тесты и документация:** smoke-тесты и тексты docs/* — драфт AI, проверка и правка смысла — моя.
- **Поиск рисков и edge cases:** prompt injection, инцидентная дедупликация, «безопасное состояние = как без автоматизации» — из совместного обсуждения; AI полезен как чек-лист рисков, но границы политики (что никогда не автозакрывать) задавал я.

## Ключевые решения, принятые кандидатом

1. Программа и набор артефактов (ИИ), язык документации (русский).
2. PoC полностью оффлайн на mock-LLM — отклонены предложенные AI альтернативы (реальный LLM API с fallback; sentence-transformers для retrieval) ради воспроизводимости и тайм-бокса: у проверяющего не должно быть шага «получить API-ключ» или «скачать 500 МБ модель».
3. Скоуп: обязательные артефакты сначала, опциональные (product.md) — по остаточному времени.

## Примеры ошибок и слабых мест AI, обнаруженные в процессе

1. **Неоткалиброванный суррогат confidence.** Предложенная AI формула уверенности в PoC даёт почти одинаковое значение (0.79) для большинства «узнанных» тикетов — это стало видно при первом прогоне demo. Для PoC приемлемо (пороговая логика работает), но как есть это создаёт ложное впечатление калиброванной вероятности. Исправлено честно: ограничение зафиксировано в SELF_REVIEW и ml.md (в целевой системе — калибровка на валидации), а не спрятано.
2. **Мёртвая ветка политики в PoC.** В коде есть ветка `suggest_to_operator` для категорий «только с оператором» (payment), но на демо-данных она недостижима: любой платёжный тикет с спорной формулировкой раньше перехватывается правилом `risk=high`. Обнаружено при анализе вывода demo (ветка ни разу не сработала). Осознанно оставлено с пониманием: правило риска и должно перекрывать политику категорий; ветка отработает для нейтральных платёжных вопросов (например, «где посмотреть чек»).
3. **Склонность к раздуванию скоупа.** Первые драфты планов AI включали больше, чем нужно для 4 часов (эскизный product.md, WORKLOG, отдельный скрипт нагрузочной демонстрации). Отклонено вручную со ссылкой на раздел задания «Что не нужно делать»; итоговый скоуп — минимально достаточный набор.
4. **Общие формулировки в первых версиях доков.** Драфты местами описывали «лучшие практики вообще» вместо решений под цифры кейса; правилось требованием привязываться к вводным (200k/день, 500 мс, 150 ₽/тикет, reopen 9%).

## Что изменилось после работы с AI

Главное — скорость итераций: за тайм-бокс получилось и спроектировать, и написать работающий PoC с тестами. Но направление держалось ручными решениями: без явных ограничений (stdlib, «сомневаешься — отдай человеку», привязка к цифрам кейса) AI выдал бы более объёмное и менее честное решение.
